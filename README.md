## Running in development


1. Run `make backend` in one terminal
2. Run `make frontend` in another terminal


## Running a Development docker build


Please use `docker compose up`


## Running a Production build


Please use `docker compose -f docker-compose.prod.yml up`