[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
streamlit = "*"
pandas = "*"
plotly = "*"
statsmodels = "*"
pytest = "*"
pytest-cov = "*"
streamlit-aggrid = "*"
kaleido = "*"

[dev-packages]

[requires]
python_version = "3.10"

[scripts]
dev = "streamlit run main.py"
test = "pytest -m single -v --cov=."
fulltest = "pytest -v --cov=."
