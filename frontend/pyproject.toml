[project]
name = ""
version = "0.0.1"
requires-python = ">=3.10"
dependencies = [
    "streamlit",
    "pandas",
    "plotly",
    "statsmodels",
    "streamlit-aggrid",
    "kaleido",
]

[tool.uv]
package = false
